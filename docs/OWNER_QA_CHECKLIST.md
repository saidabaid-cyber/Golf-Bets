# Owner QA checklist — Home correction and inherited closeout

Last updated: 2026-09-11 (America/Mexico_City)

## Package and baseline

- Working branch: `phase2/full-platform`.
- Start SHA: `a68b1b1a21af7d756c4cb479f7fce1002795bb96`.
- Package: `Backyard_Home_corregida_y_pendientes_Codex.zip`.
- Package SHA-256: `F686C63E5FDA3A3476970274038DD032C8CCD766CFC170AC5C858165371B478A`.
- Baseline before applying the package: 1,379/1,379 npm tests, lint and production build passed (28 generated pages).
- Installer-equivalent safeguards: manifest hashes passed; the starting Home blob matched the package's expected blob; only the three declared application artifacts were applied. Python was unavailable, so the supplied installer was not executed; its branch, blob and SHA-256 checks were reproduced before applying the files with the repository patch mechanism.
- Shared Supabase `zhqmlpljloumldaczcfp`: read-only; no DDL, Auth user, Storage or data mutation was performed.

## Acceptance matrix

Each row records the implementation, verification level, remaining gate and the exact next action. `PASS` means the behavior was exercised at the listed level; it does not imply that a separate human, device or database gate passed.

| ID | Status | Implemented | Tested / evidence | Missing / next action |
| --- | --- | --- | --- | --- |
| H01 | PASS | Applied the delivered calm, data-aware Home, CSS module and local fairway artwork while preserving every `HomeDashboard` callback and `ActiveRoundSummary` contract. | Package contract QA 20/20; repository Home tests cover the single primary action, setup/live/review priority, zero-score active rounds and real navigation callbacks. Commit `9a6ca04`. | Recheck the exact deployed SHA in the browser after final Preview deploy. |
| H02 | PENDING_INTERACTIVE_QA | Home hides empty statistics/activity/latest-round sections, shows only one manual setup link for a new account and prioritizes any open round. Long labels use bounded layouts. | Automated empty/data/active-state tests pass; 390/430 browser hydration and screenshots are required on the final deployment. | Product owner should traverse a post-consent account with real history; automated browser QA will cover controlled public/fixture states without accepting legal declarations. |
| H03 | PASS | Onboarding retains one selectable Quick/Complete decision, one Continue CTA, blank group name and the current warm visual treatment. | Source and behavior contracts verify both cards use `aria-pressed` and that duplicate setup CTAs do not exist. | Physical iPhone visual approval remains part of device QA, not this code status. |
| U01 | PASS | Anchored search keeps results attached to the active input and scrolls it only to the nearest visible area. Equipment sheets reset their own scroll on every wizard step and restore the prior focus without moving the page behind them. Onboarding resets to its heading without summoning the keyboard. | `owner-qa-closeout`, equipment UI and anchored-search regression tests. | Repeat keyboard/viewport behavior on a physical iPhone (`PENDING_DEVICE_QA`). |
| U02 | PASS | Audited 11 dialog-bearing TSX files. Every interactive dialog exposes `ModalCloseButton`, an equivalent 44px close control, or the dedicated timed-summary close. Shared control supports Escape and safe-area top. | Modal source audit test plus lint; equipment and Ball Fit use the focus-trapping modal hook. | Physical Safari/PWA safe-area behavior is `PENDING_DEVICE_QA`. |
| E01 | PASS | Added provider-level brand facets over the full eligible catalog. Club, ball and shaft pickers now request full current+historical brand counts server-side, then use paginated model search; no first-page brand inference. | Provider tests prove the driver brand facet covers more rows than the first 50-model page and respects category/use/query/current/historical counts. | Verify final Preview API and tap-through after deploy. |
| E02 | PASS | Empty handedness metadata is treated as unknown, so a historical club can be saved after the user declares RH/LH. Verified catalog/variant restrictions are still enforced. | Unit coverage for unknown, RH-only and LH rejection. | None at code level. |
| E03 | PASS | Ball and shaft flows use brand → model → final details, server pagination, historical models, pinned saved IDs and manual fallback. Manufacturer flex labels and weight stay independent, preserving `5.5`, `F4`, `M4`, `SF505`, etc.; no generated weight/flex Cartesian records are stored. | Full equipment, master-catalog and snapshot suites pass; provider currently contains 1,276 clubs, 310 balls and 474 shafts. | Cloud reload/edit remains `PENDING_CONTROLLED_DB_APPLY`; local/browser tap-through remains in final Preview QA. |
| E04 | BLOCKED_EXTERNAL | Added optional image URL/alt/source/license fields, HTTPS-only normalization, a reusable contained product-media view and a clean fallback. Existing catalog evidence is never replaced by invented imagery. | Normalization rejects HTTP/javascript URLs and accepts sourced HTTPS metadata; fallback/error test passes. Current measured coverage: 0 sourced images across 1,276 clubs, 310 balls and 474 shafts. | Product must provide an image manifest with redistribution/hotlink terms and stable URLs. Then import it field-by-field and record coverage; do not scrape or fabricate images. |
| O01 | PASS | Native keyboard emoji accepts one Unicode grapheme; uploaded photos and legacy preset/Google paths remain compatible. The preset tab is named “Elegir avatar”, avoiding a false avatar-generation claim. GHIN remains a visible disabled official-provider placeholder. | Emoji grapheme, legacy avatar, photo processing, flag and GHIN modal tests pass. | Physical emoji keyboard is `PENDING_DEVICE_QA`; official GHIN is `BLOCKED_EXTERNAL`. |
| G01 | PENDING_CONTROLLED_DB_APPLY | New groups begin blank; Private vs Invite-only is explained. UI does not generate an insecure local token. The invitation domain already models hashed secure tokens, identity, expiry and revocation. | Group/invite domain and RLS contract tests pass locally. | Create the approved isolated Supabase branch, apply audited migrations/RLS and exercise create/copy/revoke/accept with USER_A and USER_B. |
| HCP01 | PASS | Linked/profile players use Index + player tee + slope + rating + par to create the applied Course Handicap snapshot. Guest/manual players retain manual HCP. The snapshot now freezes once a round starts or enters review. | Formula, missing-input fail-closed, guest/manual, tee-change and post-start immutability tests pass. | Authenticated cloud persistence requires the isolated Preview DB gate. |
| BET01 | PASS | Onboarding keeps the registry-driven full group template editor. Round setup retains the mature editor. Decimal Conejos, HCP rounding, Skins carry, Foursome modes and 3/6/9/18 segments, second-nine pressures, participant sets, Current Index/Sliding Personales and supplemental presses remain available. | Full Phase 1 engine/configuration suite and closeout source contracts pass; no deterministic money calculation changed. | Human visual comparison of both entry points remains `PENDING_INTERACTIVE_QA`. |
| SCORE01 | PASS | Score starts at hole par but is unconfirmed; center tap or ± confirms. Putts follows the same model and accepts 0. There is no PAR button. Optional bunker/penalty stay zero without confirm-zero UI. | Capture, iPhone-capture, lifecycle and all bet-engine regressions pass. | Physical touch/field ergonomics remain `PENDING_DEVICE_QA`. |
| ACC01 | PENDING_CONTROLLED_DB_APPLY | Delete Account requires an explicit “all information” checkbox plus typing `ELIMINAR`; the server derives user identity from the token, deletes owned media/data in dependency order, anonymizes required shared references and deletes Auth last. It never claims success after partial failure. | Local domain/endpoint/failure-order tests pass. | Run the real destructive QA only in an isolated Preview DB with disposable accounts; shared DB must remain untouched. |
| DB01 | PENDING_CONTROLLED_DB_APPLY | Seven audited Phase 1/2 migrations and an eight-file fail-closed SQL/RLS runner are versioned. | Read-only audit confirmed the only project is shared and has real users/data; migrations were intentionally not applied. | Supabase → project `The Backyard` → Branching → create persistent development branch `The Backyard Preview` after approving USD 0.01344/hour; point only this Vercel branch Preview at it; then run `npm run test:rls:preview`. |
| QA01 | PENDING_INTERACTIVE_QA | Package QA, full npm suite, lint/build and Preview/runtime checks are part of the final gate. | Local package QA 20/20; 1,391/1,391 npm tests, lint and Next production build (28 generated pages) pass. Final deployment evidence will be added after push. | Deploy the final SHA, record immutable URL/deployment, run 390/430 browser hydration and capture screenshots. |

## External and human gates

- `PENDING_CONTROLLED_DB_APPLY`: isolated Supabase branch, authenticated multiuser persistence, secure invitations and destructive account-deletion QA.
- `PENDING_INTERACTIVE_QA`: post-consent account workflows and Product Owner visual review. Automation must not accept age, arbitration or AI-processing declarations for the owner.
- `PENDING_DEVICE_QA`: physical iPhone keyboard, safe-area, PWA and touch ergonomics.
- `BLOCKED_EXTERNAL`: official GHIN API authorization, licensed catalog imagery and private Vercel log access if no project-scoped session/token is available.
- `LEGAL_REVIEW_REQUIRED`: Phase 2 social graph, invitations, geolocation/shot coordinates, notification tokens, analytics, guest linking and AI insights remain subject to the previously documented legal review; no legal text was changed here.
